interface Action {
  id: ID!
  type: String!
  date: String!
  description: String
}

type Conservation implements Action {
  id: ID!
  type: String!
  date: String!
  description: String
  elements: [Element!]!
  employee: String
  start: String
  end: String
}

type Element {
  id: ID!
  name: String
  amount: Float
  price: Float
}

type UDT implements Action {
  id: ID!
  type: String!
  date: String!
  description: String
  inspector: String!
  results: String!
  notes: String
}

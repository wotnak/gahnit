type Query {
  devices: [Device!]!
  device(id: ID!): Device!

  deviceTypes: [DeviceType!]!
  deviceType(id: ID!): DeviceType!

  customers: [Customer!]!
  customer(id: ID!): Customer!

  users: [User!]!
  user(id: ID!): User!
  me: User
}
